#include <stdio.h>
#include <ctype.h>
#include <dirent.h>
#include <fcntl.h>
#include <unistd.h>
#include <string.h>
#include <sys/types.h>
#include <sys/stat.h>
int main(int a, char** b){
  DIR *dir;
  int i, fd_self, fd;
  unsigned long time, stime;
  char cmd[256],path[256], time[256];
  fscanf(file, "%lu", &time);
  ps -p; // process active on the program
  fclose(file);
  }
 close(fd);
}
}
close(fd_self);
return 0;
}
